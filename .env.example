# Transport mode: 'http' or 'stdio'
# Use 'http' for devcontainer access, 'stdio' for local Claude Code
MCP_TRANSPORT=http

# Port for HTTP server (only used when MCP_TRANSPORT=http)
# Default: 8837 (chosen to avoid conflicts with common services)
PORT=8837